# Node20 and npm10 are required
# To Build CSS and JS files run following commands:

# 1. Go to root folder
# 2. Install dependencies
$ npm install

# 3. make a build
$ npm run build

# You will find compresed CSS and JS files in "builder/dist" folder
