import React, { memo } from "react";
import BgIndexingInfo from "../components/BgIndexingInfo";

const BgIndexingContainer: React.FC = () => {
  return <BgIndexingInfo />;
};

export default memo(BgIndexingContainer);
